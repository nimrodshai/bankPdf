name = "telegram-bot-bridge"
main = "worker.js"
compatibility_date = "2024-01-01"

# Environment variables are set via Cloudflare dashboard (Settings > Variables)
# Required secrets:
# - GITHUB_TOKEN
# - GITHUB_OWNER
# - GITHUB_REPO
# - TELEGRAM_SECRET (optional)
